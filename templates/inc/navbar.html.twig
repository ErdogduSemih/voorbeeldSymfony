<nav class="navbar navbar-expand-lg navbar-dark bg-dark" style="margin-bottom: 2em">
    <a class="navbar-brand" href="/">Werkpakket 3</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <div class="navbar-nav">
            <ul class="navbar-nav mr-auto">
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link" href="/user">Users</a>
                    </li>
                {% endif %}

                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="/message">Messages</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/quit">Logout</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Login</a>
                    </li>
                {% endif %}
            </ul>
            <form class="form-inline my-2 my-lg-0" method="get" action="{{ path('message_search') }}">
                <input class="form-control mr-sm-2" name="search" id="search" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-primary" type="submit">Search</button>
            </form>
        </div>
    </div>
</nav>